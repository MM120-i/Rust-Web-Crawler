// Placeholder for a local fixture HTTP server that serves synthetic linked pages,
// used to drive the deterministic crawl gates (see docs/gates.md: "100 linked local
// pages", "10,000 local pages"). Real content lands in a later phase.
fn main() {
    println!("crawler-test-site placeholder");
}
